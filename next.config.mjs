/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      'randomuser.me',
      'images.unsplash.com',
      'placehold.co'
    ],
  },
};

export default nextConfig;